record(throttle, "$(P)$(THR)")
{
	field( DESC,  "throttle record")
        field( PREC,  "$(PREC=6)")
        field( DPREC, "$(DPREC=3)")
        field( OUT,   "$(OUT=)")
        field( DLY,   "$(DELAY=1.0)")
        field( DRVH,  "$(DRVH=0.0)")
        field( DRVL,  "$(DRVL=0.0)")
}
